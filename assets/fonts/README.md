Place Museo Sans font files here.

Files to add (recommended names):
- fontspring-demo-museosans_300.woff2
- fontspring-demo-museosans_500.woff2
- fontspring-demo-museosans_700.woff2

After copying the real .woff2 files into this folder, hard-refresh the site (Ctrl+F5) to load the fonts.

If you want, I can update the CSS to use a single `MuseoSans` family name (so you can use `font-weight`). Tell me if you'd like that.